# esp32-box-morse
A code to send Morse code through LEDs
O código cria um webserver wifi que permitirá modificar as frases de cada LED, rodando no core principal.
É configurado outro core para ordenar a piscada do LED curta e a longa com tasks.

A mudança de frase é feita após reinicializar as frases
Feito com Arduino IDE

